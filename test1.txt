Проект — термин, который означает временное предприятие, направленное на создание
уникального продукта, услуги или результата.

Ватутин Алексей

#include <stdio.h> 
#include <math.h> 
#include <stdlib.h>


void main()
{
	system("Chcp 1251");
	float x, y;
	printf("Введите x \n");
	scanf("%f", &x); 
	if (x > 12.5) 
		y = pow(x, 2) - 3 * x + 2.5 * pow(x, 2);
	else if (x >= 0 && x <= 1) 
		y = exp(x) + 5 + cos(0.001*x);
	else if (x < 12.5)
		y = pow(x, 2); 
	printf("y = %1.2f\n", y);
}
Программа для решения системы уравнения, написанная языке программирования C++.
C++ — язык программирования, который сочетает свойства как высокоуровневых, так и низкоуровневых языков. Отличается поддержкой нескольких парадигм программирования (процедурного, объектно-ориентированного, обобщённого и функционального), а также высоким уровнем контроля над ресурсами и производительностью.
